// import { createReducer, on } from '@ngrx/store';
// import { changeVariable } from '../actions/variable.actions';

// export const initialState = 'Initial Value';

// const _variableReducer = createReducer(
//     initialState,
//     on(changeVariable, (state, { newVariable }) => newVariable)
// );

// export function variableReducer(state, action) {
//     return _variableReducer(state, action);
// }
